<% if @course.errors.any? %>
$('.form-alert').addClass('alert-danger').html('<%= escape_javascript(errors_to_message_string(@course.errors)).html_safe %>')
<% else %>
$('.form-alert').addClass('alert-success').html('<%= escape_javascript( flash[:success] ).html_safe %>')
<% end %>
